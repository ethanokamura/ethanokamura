---
interface Props {
  title: string,
}

const { title } = Astro.props;
---

<section class="h-full flex flex-col py-20 items-center gap-10"> 
  <h1 class="text-center mb-6" set:text={title}/>
  <slot/>
</section>